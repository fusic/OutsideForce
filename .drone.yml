build:
  image: sadapon2008/centos6-remi-php56-pgdg-postgresql94
  commands:
    - curl -sS https://getcomposer.org/installer | php
    - php composer.phar install --prefer-dist --no-interaction --no-progress
    - ./vendor/bin/phpunit
